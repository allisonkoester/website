# website
Allison Koester's Website
git init
git add index.html          # plus photo.jpg and CV.pdf once you add them
git commit -m "Initial site"
git branch -M main
git remote add origin https://github.com/<username>/website.git
git push -u origin main
